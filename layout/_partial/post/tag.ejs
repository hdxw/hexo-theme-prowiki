<% if (post.tags && post.tags.length){ %>
  <div class="post_tags">
    <%- list_tags(post.tags, {
      show_count: false,
      transform(str) {
        return '<i class="fas fa-hashtag fa-fw" aria-hidden="true"></i>'+str;
      }
    }) %>
  </div>
<% } %>
